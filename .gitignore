# Build trees
build/
build-*/
cmake-build-*/
CMakeUserPresets.json

# Compiled objects and binaries
*.o
*.obj
*.a
*.so
*.so.*
*.dylib
*.dll
*.exe

# Sweep output. Everything under experiments/results is generated except the
# committed canonical summary, which the report and CI build from.
experiments/results/*
!experiments/results/.gitkeep
!experiments/results/summary.csv
!experiments/results/session_manifest.json

# Report intermediates. The published PDFs live under assets/reports/ and are
# tracked; everything LaTeX generates on the way there is not.
report/build/
report_debug/build/
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.toc
*.lof
*.lot
*.synctex.gz
*.run.xml
*.bcf
report/main.pdf
report_debug/debug_report.pdf

# Generated report figures and tables. Regenerated from summary.csv by
# scripts/gen_report_assets.py, so they are outputs rather than sources.
report/figures/*.pdf
report/figures/*.png
report/tables/*.tex

# Private working material, kept locally and deliberately not published.
report_for_me/
docs/BUILD_SPECIFICATION.md

# Python
__pycache__/
*.py[cod]
.venv/
.ruff_cache/
.pytest_cache/

# Editor, tooling and OS
.vscode/
.idea/
.cache/
*.swp
.DS_Store
Thumbs.db
