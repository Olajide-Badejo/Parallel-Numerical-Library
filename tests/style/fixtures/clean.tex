\documentclass{article}
% This fixture must pass. Everything below is a legitimate use.
\begin{document}
Prose that says 1 to 20 rather than using a range dash.
A URL is code: \url{http://example.com/a--b}.
An inline verb span: \verb|--oversubscribe|.
\begin{verbatim}
mpirun --oversubscribe -n 4 ./pnl --solver jacobi
\end{verbatim}
\begin{lstlisting}
cmake --build build --target all
\end{lstlisting}
A hyphenated word such as red black is written without a hyphen here anyway.
\end{document}
